export class Client {
    id?: number; // le ? Rend optionnel le champ. // l'id du client dans la base de donnée
    identifiant: string; // identifiant du client
    mdp: string;  // mot de passe du client
}
